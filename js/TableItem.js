export default class TableItem{
    constructor(user_name, color) {
        this.user_name = user_name; //
        this.error = error;  //цвет дела
        this.now = new Date; // дата создания и отсюда будет сгенерирован id
    }
}